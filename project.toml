name = "Zero"
version = "0.1.0"
type = "exec"
language = "cpp"

[sources]
dirs = ["src"]

[includes]
dirs = ["include", "external","external/Vulkan-Headers/include"]

[build]
flags = ["-Wall", "-Wextra", "-Wpedantic"]
optimization = "2"
libs = ["glfw", "vulkan"]
[deps]

[scripts]
# Custom build scripts can be defined here
# prebuild = "echo 'Running prebuild'"
# postbuild = "echo 'Build complete'"


