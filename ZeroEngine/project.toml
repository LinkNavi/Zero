name = "ZeroEngine"
version = "0.1.0"
type = "exec"
language = "cpp"

[sources]
dirs = ["src"]

[includes]
dirs = ["include", "include/sokol"]

[build]
flags = ["-Wall", "-Wextra", "-Wpedantic"]
optimization = "2"
libs = ["GL", "X11", "Xi", "Xcursor", "dl", "pthread"]

[deps]

[scripts]
# Custom build scripts can be defined here
# prebuild = "echo 'Running prebuild'"
# postbuild = "echo 'Build complete'"
